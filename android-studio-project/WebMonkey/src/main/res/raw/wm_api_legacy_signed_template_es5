var GM_startIntent = function(action, data, type) {
  {{jsBridgeName}}.startIntent(
    {{defaultSignature}}, action, data, type, Array.prototype.slice.call(arguments, 3)
  );
};

var GM_loadUrl = function(url) {
  {{jsBridgeName}}.loadUrl(
    {{defaultSignature}}, url, Array.prototype.slice.call(arguments, 1)
  );
};
